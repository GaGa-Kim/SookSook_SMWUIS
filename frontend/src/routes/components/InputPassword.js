import { Input, Space } from "antd";
const InputPassword = () => {
    return (
        <Input.Password
            placeholder="입력하세요"
            style={{ "border-radius": "70px" }}
        />
    );
};

export default InputPassword;
